(()=>{const K='lcr_attribution_v1',keys=['utm_source','utm_medium','utm_campaign','utm_content','utm_term','fbclid'];let saved={};try{saved=JSON.parse(localStorage.getItem(K)||'{}')||{}}catch{}const p=new URLSearchParams(location.search);let changed=false;for(const k of keys){const v=p.get(k);if(v){saved[k]=v.slice(0,250);changed=true}}if(changed){saved.first_seen_at=saved.first_seen_at||new Date().toISOString();saved.last_seen_at=new Date().toISOString();try{localStorage.setItem(K,JSON.stringify(saved))}catch{}}window.LCR_ATTRIBUTION={get(){try{return JSON.parse(localStorage.getItem(K)||'{}')||{}}catch{return{}}},append(base){const a=this.get();const u=new URL(base,location.origin);for(const k of ['utm_source','utm_medium','utm_campaign','utm_content','utm_term'])if(a[k]&&!u.searchParams.has(k))u.searchParams.set(k,a[k]);return u.toString()}}})();